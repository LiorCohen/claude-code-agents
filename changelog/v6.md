# Changelog - Version 6.x

All notable changes for major version 6 of the SDD plugin.

---

## [6.7.0] - 2026-02-10

### Added

- **scaffolding-engine**: Generic declarative scaffolding engine in system CLI (`scaffolding apply --spec <path>`)
  - 5 operation types: `template_dir`, `template_file`, `mkdir`, `write_file`, `package_json_scripts`
  - Conditional operations via `when` clauses with `equals` and `not_empty` operators
  - Non-destructive by default (`if_exists: "skip"`), opt-in overwrite
  - Variable substitution with `{{VAR}}` syntax and built-in `DATE`/`DATE_TIME` variables
  - Dry-run support (`--dry-run` flag)
  - JSON Schema validation for spec format
- **engine tests**: 35 unit tests for pure functions, 19 integration tests via CLI

### Changed

- **scaffolding/project.ts**: Refactored to use engine internally — builds a `ScaffoldSpec` and delegates to `executeSpec`
- **component skills**: All 6 component scaffolding skills updated with declarative spec sections (Tier 1-3)
- **orchestrator skill**: Updated to describe engine-based scaffolding workflow
- **project-scaffolding skill**: Updated to reference engine integration

### Removed

- **npm-run-all**: Removed from project template devDependencies (meta-scripts dropped)
- **meta-scripts**: Removed `generateMetaScripts()` and orchestration scripts (`dev`, `build`, `test`, `start`)

### Rationale

Scaffolding logic was split between the system CLI (file operations) and component skills (conditional logic in prose). The engine unifies deterministic operations into a single declarative spec, reducing LLM interpretation errors and enabling dry-run validation.

---

## [6.6.4] - 2026-02-10

### Changed

- **system-cli-standards**: Streamline skill — consolidate "What NOT to do" table into single bullet, simplify audit procedure to read-based approach with clearer finding categories
- **spec-index**: Remove CI automation section with non-functional `system-run.sh` workflow example

---

## [6.6.3] - 2026-02-09

### Fixed

- **system-cli-standards**: Fix all 47 CLI invocation violations across 13+ prompt files
  - Replace bare `sdd-system`, `npx sdd-system`, and `node ... cli.js` with canonical `system-run.sh` pattern
  - Remove false "Available in PATH" claims and Prerequisites sections
  - Convert temp-file patterns to stdin pipe (`--config -`) convention
  - Remove broken npm scripts from scaffolded templates (database, contract) — `${CLAUDE_PLUGIN_ROOT}` doesn't exist in scaffolded projects
  - Update all downstream references (`/sdd-run` for user-facing, `system-run.sh` for plugin internals)
  - Fix database and contract template tests to match updated templates

### Rationale

The system CLI standards skill (6.6.2) defined the canonical invocation pattern but existing prompt files still used legacy patterns. This release enforces consistency across all skills, commands, agents, and templates.

---

## [6.6.2] - 2026-02-09

### Added

- **system-cli-standards**: New standards skill defining the canonical way to invoke the system CLI from prompt files
  - Documents invocation pattern via `system-run.sh`, output contracts (`CommandResult`), authority boundaries, and stdin convention
  - Includes audit procedure and violation categories
- **system-run.sh**: New wrapper script as single entry point for all prompt-to-CLI invocations
  - Lives at `plugin/system/system-run.sh`, invoked via `${CLAUDE_PLUGIN_ROOT}/system/system-run.sh`
  - Replaces direct `node ... cli.js` invocations in prompt files

### Changed

- **skills-standards**: Added cross-reference to `system-cli-standards` in Layer Separation section
- **agents-standards**: Added CLI Delegation section referencing `system-cli-standards`
- **commands-standards**: Added cross-reference to `system-cli-standards` in CLI Integration section
- **CLAUDE.md**: Added `system-cli-standards` to Skills list and Repository Structure

---

## [6.6.1] - 2026-02-09

### Added

- **settings CLI**: New `sdd-system settings reconcile` command for programmatic settings reconciliation
  - Reads `.sdd/sdd-settings.yaml`, runs reconciliation, writes result back
  - Outputs summary of changes and directory warnings
  - Supports `--json` output mode

### Fixed

- **sdd-init**: Fix build paths in Phase 0 and Phase 2.2 to use `system/` directory
  - Both phases now run `npm install` and `npm run build` in `${CLAUDE_PLUGIN_ROOT}/system/` instead of non-existent root `package.json`
  - Fixes ENOENT error when running sdd-init on projects with version mismatch
- **sdd-init**: Fix Phase 2.7 reconciliation to use CLI command instead of fragile `node -e` invocation
  - Phase 2.7 now invokes `sdd-system settings reconcile` which resolves modules correctly

---

## [6.6.0] - 2026-02-09

### Added

- **settings**: New `reconcileSettings()` module for automatic settings migration during plugin upgrades
  - Migrates `sdd.plugin_version` → split `initialized_by_plugin_version` + `updated_by_plugin_version`
  - Migrates `sdd.last_updated` → `updated_at` with full UTC datetime format
  - Removes deprecated `project.domain` and `project.type` fields
  - Adds missing component `path` fields (infers from filesystem or generates)
  - Adds `system.logging` section when missing
  - Detects and reports directory structure mismatches
- **sdd-init**: Phase 0 (version detection + plugin build) and Phase 2.7 (settings reconciliation)
  - Detects existing projects and skips redundant prompts
  - Reconciles old-format settings to latest schema on version mismatch

### Changed

- **types**: `SddMetadata` now uses split version fields and UTC datetime format
  - `plugin_version` → `initialized_by_plugin_version` (immutable) + `updated_by_plugin_version`
  - `last_updated` → `updated_at` (YYYY-MM-DD HH:MM:SSZ format)
- **types**: `ProjectMetadata` simplified — `domain` and `type` removed, `description` made optional
- **schema**: Both `schema.ts` and `sdd-settings.schema.json` updated to latest format only
- **sdd-version**: Reads from `updated_by_plugin_version` with legacy fallback, shows `initialized_by_plugin_version`
- **sdd-change**: Removed `default_domain` from spec-decomposition invocation (domain inferred from spec content)
- **cli**: `loadSettings()` replaced with safe `loadLoggingConfig()` that works with pre-reconciled files
- **project-settings skill**: Updated schema docs, examples, minimal template, and create operation params

### Rationale

When users update the SDD plugin, their existing `sdd-settings.yaml` may use older field names or be missing sections added in newer versions. Rather than a heavyweight migration framework, the reconciler uses a "translate to latest" approach — it reads any older format and produces output conforming to the current schema. This keeps settings reconciliation simple and avoids maintaining a migration chain.

---

## [6.5.0] - 2026-02-09

### Added

- **commands**: New `/sdd-version` command
  - Shows the installed plugin version from `plugin.json`
  - Shows the project's plugin version from `sdd-settings.yaml`
  - Highlights version mismatches between installed and project versions

---

## Infrastructure - 2026-02-09

### Changed

- **skills-standards**: Updated frontmatter documentation to include all supported fields
  - Corrected outdated restriction that only allowed `name`, `description`, and `user-invocable`
  - Added comprehensive documentation for optional fields: `model`, `disable-model-invocation`, `argument-hint`, `allowed-tools`, `context`, `agent`, `hooks`
  - Updated checklist to reflect optional nature of frontmatter fields
  - Aligned with official Claude Code 2.1.27 documentation
- **tasks skill**: Added `model: opus` to use Opus for task management operations
  - Ensures task operations use the most capable model for complex git workflows and file management

### Removed

- **tests**: Removed useless benchmark tracking system
  - Deleted `tests/src/lib/benchmark.ts` (414 lines) and associated unit tests
  - Removed all benchmark data YAML files from `tests/data/`
  - Removed `recordBenchmark()` calls from workflow tests
  - Cleaned up exports from test library index

### Added

- **tests/.gitignore**: Prevent test artifacts from polluting source directories
  - Added `.sdd/` and `changes/` to gitignore (workaround for #111)
- **plugin/system/.gitignore**: Prevent test artifacts in CLI system directory
  - Added `.sdd/` and `changes/` patterns

### Tasks

- **#111**: Created task to properly fix test suite pollution issue
  - Despite commit 5cfff56, tests still create `.sdd/system-logs/` in source dirs
  - Gitignore is a temporary workaround, root cause needs investigation

---

## [6.4.0] - 2026-02-09

### Added

- **plugin/system/settings**: System settings with logging configuration
  - Added `SystemSettings` and `LoggingSettings` types to support CLI system configuration
  - Extended settings schema with optional `system.logging` section (enabled, level)
  - Integrated system settings from task #110

- **plugin/system/settings**: Testing and CI/CD component types
  - Added `TestingSettings` and `CicdSettings` types (empty objects for now)
  - Added `TestingComponent` and `CicdComponent` interfaces
  - Added type guards: `isTestingComponent`, `isCicdComponent`
  - Extended schema validation for testing and cicd components

- **plugin/system/settings**: Component path field for backwards compatibility
  - Added required `path` field to `ComponentBase` interface
  - Supports both flat structure (`components/{name}`) and type-based folders (`components/{type-plural}/{name}`)
  - Added `generateComponentPath()` helper function for conventional path generation
  - Simplified `getComponentDir()` to return stored path directly

### Changed

- **plugin/system/settings**: Optional fields philosophy - "reflect what IS, not what should be"
  - Made `helm` optional with false default (was implicitly required)
  - Made `databases`, `provides_contracts`, `consumes_contracts`, `contracts` optional with empty array defaults
  - Removed empty array initializations from default settings objects
  - Added `?? []` nullish coalescing fallbacks throughout codebase for safe array access
  - Updated all schemas (TypeScript types, TypeScript JSON schema, JSON schema file) consistently
  - Corrected descriptions: "SDD CLI system settings" (not "System-wide settings")

### Rationale

The settings system had accumulated inconsistencies between three schema representations and lacked support for recent additions (system settings, testing/cicd types). The core philosophy shift—settings should reflect actual project state rather than idealized structure—led to making optional fields truly optional with sensible defaults. This enables minimal configurations while supporting backwards compatibility through explicit path storage. The breaking test failures are intentional and will be fixed in the next commit.

---

## Infrastructure - 2026-02-08

### Changed

- **tasks skill**: All task transition commits now invoke the commit skill instead of creating commits directly
  - Ensures proper Co-Authored-By attribution on all task commits
  - Maintains consistency with repository commit standards
  - Eliminates code duplication between tasks and commit skills

### Rationale

The tasks skill was creating commits directly without Co-Authored-By attribution, causing non-compliant commits. By deferring to the commit skill for all transitions (add, plan, ready, implement, review, complete, reject, consolidate, prioritize), we ensure all task commits follow repository standards and maintain a single source of truth for commit procedures.

---

## [6.3.7] - 2026-02-08

### Added

- **plugin/system**: Structured logging using pino library
  - Logs written to `.sdd/system-logs/system-YYYY-MM-DD.log` with date partitioning
  - Log every CLI invocation with namespace, action, arguments, and result
  - Include timestamp, PID, and Claude session ID (from `CLAUDE_SESSION_ID` env var) in all log entries
  - Settings schema extended with `system.logging` section (enabled: boolean, level: string)
  - Default: enabled=true, level=info
  - `npm run logs` script for tailing logs with pino-pretty formatting
  - File logger independent of console output (console remains user-facing, logging is for audit/debug)

### Rationale

The plugin system lacked structured logging infrastructure for debugging and auditing. Console output serves user-facing feedback but doesn't record system events for later analysis. Pino provides structured JSON logging to `.sdd/system-logs/` with configurable levels, enabling troubleshooting and understanding system behavior without affecting console output.

---

## [6.3.6] - 2026-02-08

### Added

- **commands-standards**: New Argument Handling section with rules for zero-argument behavior and focused usage guides
  - Commands must show tailored usage guides when called with insufficient arguments
  - Examples for multi-action commands and zero-argument commands
  - Guidelines for contextual help based on what's missing

### Enhanced

- **sdd-config**: Add "When Called Without Arguments" section showing comprehensive usage guide with all operations
- **sdd-run**: Add "When Called Without Arguments" section showing usage guide with namespaces and global options
- **sdd-change**: Add "When Called Without Arguments" section with COMMON WORKFLOWS examples
- **sdd-settings**: Convert file paths in output examples to clickable markdown links
- **sdd-run**: Convert config path to markdown link format

### Fixed

- **commands-standards**: Remove unsupported `user-invocable` frontmatter field

### Rationale

Users invoking commands without required arguments previously received unclear or no guidance. The new argument handling standards ensure all multi-action commands display focused, contextual usage guides that help users understand what's available and how to proceed. This improves discoverability and reduces friction when learning command syntax.

---

## [6.3.5] - 2026-02-08

### Enhanced

- **sdd-change command**: Convert all file path references in output examples to clickable markdown links (`[filename.ext](path/to/file.ext)`) to enable click-to-navigate in VSCode terminals
- **commands-standards**: Add markdown link formatting conventions to output formatting section, documenting the required syntax for file, directory, change ID, and task references

### Rationale

Plain-text file paths in command output forced users to manually navigate to files. By using markdown link syntax relative to the repo root, all file references become clickable in VSCode terminals, improving navigation efficiency during `sdd-change` workflows.

---

## [6.3.4] - 2026-02-08

### Enhanced

- **plan templates**: Add `Standards:` field to every phase in feature, bugfix, and refactor plan templates, listing which standards skills apply to each agent's work
- **plan templates**: Make task descriptions methodology-aware (CMDO, MVVM, TDD, contract-standards) instead of generic checklists
- **planning skill**: Update Plan Content Guidelines to include standards references as acceptable plan content
- **planning skill**: Add standards column to agent assignment table and update all three template sections to match

### Rationale

Plans are the execution script for `sdd-change implement`. Without explicit standards references, there was no contract between the plan and the standards — agents probably followed them (wired in via frontmatter), but the plan didn't reinforce it. This also made plans unreadable for human reviewers who couldn't tell which standards applied without reading agent definitions. Now the flow is: Plan (standards-aware) → Execute (standards explicitly scoped) → Verify (confirm compliance).

---

## [6.3.3] - 2026-02-08

### Fixed

- **plugin manifest**: Add explicit `skills` paths for component subdirectories so nested skills (backend-standards, frontend-standards, database-standards, etc.) are discoverable after plugin installation

### Rationale

Component skills nested under `skills/components/*/` were not showing up because Claude Code's plugin skill discovery only scans directories declared in the manifest. Adding the component subdirectory paths to the `skills` array makes all 34 skills discoverable without flattening the directory structure.

---

## [6.3.2] - 2026-02-07

### Fixed

- **agents**: Wire missing standards skills into agents that should reference them
  - `db-advisor`: add `database-standards`
  - `devops`: add `cicd-standards`
  - `api-designer`: add `contract-standards`
  - `tester`: add `testing-standards`
  - `backend-dev`: add `database-standards`
- **standards skills**: Remove `user-invocable: false` from all 10 standards skills so they are accessible during direct interaction (not only via agent preloading)
- **CLAUDE.md template**: Add mandatory coding standards section so Claude follows standards during direct prompts, corrections, and ad-hoc requests — not just agent-driven workflows

### Rationale

Standards skills existed but were largely invisible: 5 of 10 were never referenced by any agent, all were marked `user-invocable: false` (inaccessible during direct conversation), and the scaffolded CLAUDE.md had no mention of them. This caused Claude to ignore project standards whenever users typed corrections or prompts outside of agent workflows.

---

## [6.3.1] - 2026-02-07

### Fixed

- **root package.json**: Fix workspace glob `components/*` to `components/*/*` for nested component directories
- **changes/INDEX.md template**: Remove non-functional "External Specifications" section from change-creation template
- **sdd-change verify**: Add standards verification step that checks implementation against referenced skills
- **external-spec-integration**: Remove all archived-spec file references from downstream workflows (10 files)
- **sdd-init**: Fix incorrect `.claude/settings.json` format to use proper `extraKnownMarketplaces` and `enabledPlugins` structure

### Changed

- **frontend-scaffolding**: Upgrade to Tailwind CSS v4 — eliminates `tailwind.config.js`, `postcss.config.js`, and `tsconfig.node.json` (8 root config files → 5)
- **agents**: Add `skills` frontmatter field to all 7 agents so skill content is preloaded into agent context at startup
  - Without this field, agents could see skill names but actual skill content was never injected
- **agents-standards**: Document `skills` as a required frontmatter field in the agent template

### Rationale

These fixes address real-world issues discovered during plugin usage. The agent skills frontmatter fix is the most impactful — per Claude Code docs, subagents don't inherit skills from the parent conversation and must list them explicitly in frontmatter for content injection.

---

## [6.3.0] - 2026-02-07

### Added

- **env/check-tools**: New CLI command (`sdd-system env check-tools`) that checks all required development tools
  - Returns structured JSON with tool name, installed status, version, and install hints
  - Detects platform (macOS/Linux/WSL) and package manager (brew/apt-get/dnf/yum)
  - Platform-appropriate install hints for missing tools
  - Human-readable and JSON output modes
  - Blocks unsupported platforms (native Windows) with WSL install instructions

### Changed

- **sdd-init**: Restructure environment verification phases
  - Plugin installation verification is now the first check (hard blocker)
  - Tool checking delegated to `sdd-system env check-tools` (single CLI call replaces 7+ individual commands)
  - Added `.claude/settings.json` verification for SDD marketplace and plugin entries
  - Renumbered phases 0–4 to 1–5 for consistency
  - Added session restart notice to completion message
  - Removed redundant sections: "Available Component Types", "Important Notes", "Non-Destructive Behavior", duplicate "NOT done during init" lists

### Rationale

Plugin verification must happen before tool checks — nothing works without it. Delegating tool checking to the CLI is faster, more reliable, and eliminates prompt-based version extraction. Deduplication ensures every fact appears exactly once.

---

## [6.2.4] - 2026-02-07

### Fixed

- **project-settings**: Remove consumer references (self-containment violation)
  - Removed `## How to Reference This Skill` section (listed callers)
  - Removed `## Related Commands` section (listed invoking commands)
  - Removed caller reference from Minimal Template heading

---

## [6.2.3] - 2026-02-07

### Fixed

- **project-settings**: Fix skills-standards violations
  - Renamed H1 from "Project Settings Skill" to "Project Settings" (must match `name` in title-case)
  - Replaced blockquote with one-line summary paragraph
  - Moved `## Input` / `## Output` before core sections per standard structure
  - Added `markdown` language specifier to two bare code blocks

---

## [6.2.2] - 2026-02-07

### Fixed

- **project-settings**: Add missing testing and cicd component types to schema and documentation
  - Added `if/then` blocks in `sdd-settings.schema.json` to constrain testing/cicd settings to empty objects
  - Added Testing Settings and CI/CD Settings sections to SKILL.md
  - Added testing and cicd directory mappings to the directory structure table
  - Cleaned up leftover task files from incomplete status move

### Rationale

The JSON Schema declared `testing` and `cicd` in the component type enum but had no `if/then` constraints for their settings and no documentation in SKILL.md. This left them unconstrained and undocumented.

---

## [6.2.1] - 2026-02-07

### Refactored

- **project-settings**: Centralize all sdd-settings schema ownership
  - Enhanced project-settings skill as the single authoritative source for schema, validation, and directory mappings
  - Added "Authoritative Source" declaration, "Minimal Template" section, and "How to Reference This Skill" guidance
  - De-duplicated inline schema/settings tables from 21 files across skills, agents, and commands
  - Net reduction of ~294 lines of duplicated settings knowledge
  - All files now reference `project-settings` skill instead of inlining schema details

### Rationale

Many skills, agents, and commands duplicated `.sdd/sdd-settings.yaml` schema knowledge inline — component settings tables, validation rules, directory mappings, and YAML examples. This made updates error-prone and inconsistent. Centralizing ownership in the `project-settings` skill ensures a single source of truth.

---

## [6.2.0] - 2026-02-07

### Changed

- **agents**: Merge ci-dev agent into devops agent
  - CI/CD pipeline responsibilities (GitHub Actions, PR checks, build automation) absorbed into devops
  - ci-dev.md deleted — devops now owns both infrastructure and CI/CD
  - Added `## Skills` section with `postgresql` reference and `## Working Directory` section (fixes standards violations)
  - Agent count reduced from 8 to 7
  - Updated README, docs/agents.md, and marketplace.json

### Rationale

ci-dev and devops had significant overlap (both handled Testkube, build pipelines, deployment). The boundary between "infrastructure" and "CI/CD pipelines" was artificial. Merging simplifies the agent roster and eliminates the unclear responsibility split.

---

## [6.1.8] - 2026-02-06

### Removed

- **epic-planning**: Merge into change-creation skill and delete
  - Epic lifecycle, implementation workflow, and failure handling added to change-creation
  - Epic-specific schema fields (child_changes, acceptance_criteria, cross_cutting_concerns) merged into change-creation schemas
  - Updated planning and spec-decomposition skills to reference change-creation instead

### Rationale

epic-planning duplicated ~90% of change-creation's epic handling. Merging eliminates a separate skill, reduces maintenance, and follows the self-containment principle from skills-standards (no cross-skill file references).

---

## [6.1.7] - 2026-02-06

### Changed

- **change-creation**: Extract 8 inline templates into `templates/` subdirectory
  - SKILL.md reduced from 1398 to 261 lines (81% reduction)
  - Eliminates nested markdown fence rendering issues
  - Templates: spec-feature, spec-bugfix, spec-refactor, spec-epic, plan-feature, plan-bugfix, plan-refactor, plan-epic

---

## [6.1.6] - 2026-02-06

### Enhanced

- **All 35 plugin skills**: Skills standards audit compliance fixes
  - Fix 3 broken cross-references (api-design, plugin-testing-standards)
  - Add language tags to ~107 bare code blocks across 32 skills
  - Add "no input/output" note to 15 standards-only skills
  - Fix spec-solicitation description caller reference
  - Rewrite 13 Related Skills sections with delegation contracts
  - Fix 5 vague "Follow X"/"See X" cross-references
  - Remove "called by" body text from 8 skills
  - Resolve 6 informational dependency cycles (scaffolding ↔ standards)
  - Add sdd-system CLI prerequisite to 8 skills

### Added

- **Input/output schemas**: 34 colocated JSON Schema files (Draft 2020-12)
  - 14 process skills: input.schema.json + output.schema.json
  - 6 scaffolding skills: input.schema.json
  - All schemas derived from existing inline documentation
  - SKILL.md files updated with schema references

### Rationale

Audit found systemic violations across all 35 skills: no schemas existed, ~107 code blocks lacked language tags, 20 skills had self-containment issues, and 6 bilateral dependency cycles existed. Fixes improve LLM skill selection accuracy and enable future schema-based validation (#27).

---

## [6.1.5] - 2026-02-06

### Removed

- **check-tools skill**: Inline environment check into sdd-init, remove single-use skill
  - Rename concept from "check-tools" to "check environment"
  - Tool tables, detection rules, and output format now self-contained in sdd-init Phase 1.0

---

## [6.1.4] - 2026-02-06

### Removed

- **product-discovery skill**: Remove product-discovery skill and all references
  - This plugin is for technical specs, not product specs
  - Remove domain-population from speccing phase (belongs in implementation only)
  - Update component-discovery, domain-population, external-spec-integration skills
  - Update sdd-change, sdd-init commands and all documentation

### Rationale

Product discovery (personas, workflows, domain entities) is outside the scope of a technical specification tool. Domain population belongs in the implementation phase where it reads from SPEC.md content, not during speccing/planning.

---

## [6.1.3] - 2026-02-06

### Fixed

- **Skills**: Remove Python references from skill documentation (spec-index, spec-writing, frontend-scaffolding)
  - Replace `actions/setup-python` with `actions/setup-node` in CI example
  - Replace `validate-spec.py` with `npx sdd-system spec validate` command
  - Rewrite Python code example as TypeScript

---

## [6.1.2] - 2026-02-06

### Changed

- **Component skills**: Reorganize 15 component-specific skills into colocated `plugin/skills/components/<type>/` directory structure
  - backend, config, contract, database, frontend, helm, cicd, integration-testing, e2e-testing
- **Scaffolding**: Update template path references in `project.ts` and scaffolding SKILL.md
- **Tests**: Update path constants across 6 test files

---

## [6.1.1] - 2026-02-06

### Changed

- **All 37 plugin skills**: Add `user-invocable: false` frontmatter to hide internal skills from user `/` command menu
- **check-tools, local-env**: Add missing `name` and `description` frontmatter fields

---

## [6.1.0] - 2026-02-05

### Added

- **TypeScript enforcement for phase gating**: New `workflow` namespace in sdd-system CLI
  - `sdd-system workflow check-gate --target <phase>` programmatically enforces phase transitions
  - Four gate types: plan (specs approved), implement (plans approved), review (impl complete), completion (reviews approved)
  - Hierarchical item handling for epics with children
  - Stale dependency detection (`needs_rereview` status)

- **Product/Tech spec distinction**: Conditional validation based on `spec_type` field
  - Product specs: fewer required fields (no issue, no sdd_version)
  - Tech specs: full validation including `type` (feature/bugfix/refactor/epic)

- **Four-field status model**: Per-item workflow tracking
  - `spec_status`, `plan_status`, `impl_status`, `review_status`
  - Enables granular progress tracking and phase gating

- **New documentation**: `docs/external-specs.md`, `docs/workflow-progress.md`

### Changed

- **component-recommendation → component-discovery**: Renamed skill, read-only analysis
- **Skills updated**: workflow-state, spec-writing, spec-solicitation, external-spec-integration, planning, product-discovery, project-scaffolding, spec-decomposition
- **Commands updated**: sdd-change.md (new actions), sdd-init.md (component types table)
- **Validation tests**: 59 new tests for workflow check-gate, updated spec validation tests

---

## [6.0.0] - 2026-02-05

### Breaking Changes

- **Unified `/sdd-change` command**: Replaces three separate commands with a single command and subcommands
  - `/sdd-new-change` → `/sdd-change new`
  - `/sdd-implement-change` → `/sdd-change implement`
  - `/sdd-verify-change` → `/sdd-change verify`
  - New subcommands: `status`, `continue`, `list`, `approve spec`, `approve plan`

### Added

- **workflow-state skill**: New skill for managing `.sdd/workflows/` state with zero session context
  - All workflow state persisted in `workflow.yaml`
  - New session can resume with only file contents
  - Workflow IDs (6-char alphanumeric) and change IDs (`<workflow-short>-<seq>`)

- **spec-solicitation skill**: Guided 9-step requirements gathering workflow
  - Context, Functional, Non-Functional, User Stories, Acceptance Criteria
  - Edge Cases, Dependencies, Tests, Technical Architecture
  - Full Q&A trail preserved in solicitation-workflow.yaml

- **Two-stage approval workflow**: Spec review → Plan review → Implementation
  - `spec_review` → `plan_review` → `plan_approved` → `implementing` → `verifying` → `complete`
  - Implementation blocked until both spec and plan are approved

- **Domain Model section in specs**: Comprehensive domain analysis
  - Entities with definitions and spec file paths
  - Relationships between entities
  - Glossary terms with precise definitions
  - Bounded contexts identification

- **Specs Directory Changes section**: Impact analysis for specs/
  - Before/after tree visualization
  - Changes summary with action (create/modify) and description

- **Checkpoint commits**: Automatic commits on feature branches during implementation
  - Recovery point after each phase
  - Uses `--no-verify` for speed

- **API-first implementation ordering**: Contracts → Data → Backend → Frontend → Infrastructure

### Changed

- **external-spec-integration skill**: Major rewrite
  - Archives to `.sdd/archive/external-specs/` with `yyyymmdd-filename.md` format
  - Creates workflow items with context files, not full specs
  - Includes thinking step with domain analysis

- **change-creation skill**: Added new spec sections
  - Domain Model, Specs Directory Changes, Components, System Analysis, Requirements Discovery

- **spec-decomposition skill**: Added thinking step
  - Domain extraction, gap analysis, API-first ordering
  - Outputs domain_model, specs_impact, gaps_identified, api_first_order

- **spec-writing skill**: Added new required sections
  - Domain Model, Specs Directory Changes, Components, System Analysis, Requirements Discovery, Testing Strategy

- **planning skill**: Workflow integration
  - Plan creation triggered by spec approval via `/sdd-change approve spec`

- **PERMISSIONS.md hook**: Added `.sdd/` and `changes/` to safe directories

- **Documentation**: Updated all command references
  - README.md, docs/commands.md, docs/getting-started.md
  - docs/workflows.md, docs/tutorial.md, docs/agents.md

- **Templates**: Updated project scaffolding templates
  - project/CLAUDE.md, project/README.md

- **Test files**: Renamed to match new command structure
  - `sdd-new-change.test.ts` → `sdd-change-new.test.ts`
  - `sdd-new-change-external-spec.test.ts` → `sdd-change-new-external.test.ts`

### Removed

- `/sdd-new-change` command (use `/sdd-change new`)
- `/sdd-implement-change` command (use `/sdd-change implement`)
- `/sdd-verify-change` command (use `/sdd-change verify`)

### Migration Guide

| Old Command | New Command |
|-------------|-------------|
| `/sdd-new-change --type feature --name X` | `/sdd-change new --type feature --name X` |
| `/sdd-new-change --spec path` | `/sdd-change new --spec path` |
| `/sdd-implement-change dir` | `/sdd-change implement <change-id>` |
| `/sdd-verify-change dir` | `/sdd-change verify <change-id>` |

New workflow commands:
- `/sdd-change status` - Show current workflow status
- `/sdd-change continue` - Resume current workflow
- `/sdd-change approve spec <id>` - Approve spec, create plan
- `/sdd-change approve plan <id>` - Approve plan, enable implementation
- `/sdd-change list` - List all active workflows

### Rationale

This major version introduces a zero-session-context architecture where all workflow state is persisted in `.sdd/workflows/`. This enables:
- Session recovery without conversation history
- Multi-user safety with workflow-scoped change IDs
- Two-stage approval for quality gates before implementation
- Comprehensive domain analysis during spec creation
